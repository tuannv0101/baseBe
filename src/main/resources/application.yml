spring:
  application:
    name: backend-base
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://103.216.117.60:3306/testdb_spring?allowPublicKeyRetrieval=true&useSSL=false
    username: anhvt3010
    password: '@fIP1pQWIiB-Jol6U^W)'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:v5ZzrMHqRFiZqdXgs6JWJcmSsGgQ3qTqdIS/jkt+iGoAI+EOzJMGbGlj2hVL7+ogx936DX8mVfntfPEjFsAWDw==}
  legacy-secret: ${JWT_LEGACY_SECRET:}
  expiration: 86400000 # 24 hours

file:
  # Local file storage root. Override via env var FILE_UPLOAD_DIR when needed.
  upload-dir: ${FILE_UPLOAD_DIR:D:/data/test/fileUpload/}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
